# -*- coding: utf-8 -*-
"""Freecodecamp - Pandas

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1VveoQHPIbt1UO2zjmOFQuRaeU-rg-NF5

**Pandas Series**
"""

import pandas as pd
import numpy as np

g7_pop = pd.Series([35.467, 63.951, 80.940, 60.665, 127.061, 64.511, 318.523])

g7_pop

g7_pop.name = 'População no G7 em milhões'

g7_pop

g7_pop.dtype

g7_pop.values

type(g7_pop.values)

g7_pop

g7_pop[0]

g7_pop[1]

g7_pop.index

l = ['a', 'b', 'c']

g7_pop.index = [
    'Canadá',
    'França',
    'Alemanha',
    'Itália',
    'Japão',
    'Reino Unido',
    'Estados Unidos',
]

g7_pop

pd.Series({
    'Canadá': 35.467,
    'França': 63.951,
    'Itália': 60.665,
    'Japão': 127.061,
    'Reino Unido': 64.511,
    'Estados Unidos': 318.523
    }, name = 'População no G7 em milhões')

pd.Series(
    [35.467, 63.951, 80.94, 60.665, 127.061, 64.511, 318],
    index=['Canadá', 'França', 'Alemanha', 'Itália', 'Japão', 'Reino Unido', 'Estados Unidos'],
    name = 'População no G7 em milhões')

pd.Series(g7_pop, index=['França', 'Alemanha', 'Itália', 'Espanha'])

"""**Index**"""

g7_pop

g7_pop['Canadá']

g7_pop['Japão']

g7_pop.iloc[0]

g7_pop.iloc[-1]

g7_pop[['Itália', 'França']]

g7_pop.iloc[[0, 1]]

g7_pop['Canadá': 'Itália'] #recorte estabelecido em pandas

"""**Seleção com condicionais (boolean arrays)**"""

g7_pop

g7_pop > 70

g7_pop[g7_pop > 70] #retorna os países de acordo com a condição

g7_pop.mean()

g7_pop[g7_pop > g7_pop.mean()] #retorna os países segundo a condição de que seja maior do que a média

g7_pop.std() 

#calcula o desvio padrão dos dados fornecidos
#desvio padrão (SD) é medido como a disseminação da distribuição de dados em um determinado conjunto de dados

g7_pop[(g7_pop > g7_pop.mean() - g7_pop.std() / 2) | (g7_pop > g7_pop.mean() + g7_pop.std() / 2)]

"""**Métodos e operações**"""

g7_pop

g7_pop * 1_000_000

g7_pop.mean()

np.log(g7_pop)

g7_pop['França': 'Itália'].mean()

"""**Boolean arrays**"""

g7_pop

g7_pop > 80

g7_pop[g7_pop > 80]

g7_pop[(g7_pop > 80) | (g7_pop < 40)]

g7_pop[(g7_pop > 80) & (g7_pop < 200)]

"""**Modificando séries**"""

g7_pop['Canadá'] = 40.5

g7_pop

g7_pop.iloc[-1] = 500

g7_pop

g7_pop[g7_pop < 70]

g7_pop[g7_pop < 70] = 99.99

g7_pop

